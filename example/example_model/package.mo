within;
package example_model
  // 包的描述（可选）
  annotation(Documentation(info="<html>
    <p>This is an example package for tricys, containing example models for tritium integrated cycle simulation.</p>
  </html>"));
end example_model;